# MaxHold 額外五輪指標研究預登記（2026-09-23）

本文件先於本批候選回測結果建立。沿用前輪已驗證的完整狀態引擎與凍結資料，不改策略主程式、設定、交易資料或部署。這五種是**新的 MaxHold 決策時點用途**，不宣稱指標本身從未在任何舊進場研究使用。前輪主動成交方向、四棒價格效率、波動收縮，及更早的單棒強度／ATR 不利收盤均不重測。BTC 本機 1h 快取只到 2026-07-02，不能覆蓋本批基準截止 2026-09-08，故不納入。

## 共通研究設計

- 現行本機 V14+R + V25-D，L15/S15；已驗證的完整狀態引擎、historical margin schedule、realistic fills、原費用、每筆額外 $5、公開 funding 帳本與逐時 mark equity。每輪都對比 0／2／5bp 額外滑價，較晚期固定自 2026-01-01 起。基準與無候選 hook 在每種成本下逐筆 parity。
- 只在原 MaxHold 棒收盤、當時方向性浮盈 `<=0`、原 SafeNet／TP／MFE 未出場時判斷。若條件成立，最多再持有 2 根已完成 1h 棒；原出場規則、正收益 extension／BE、占用、cooldown、替代交易、熔斷與連虧冷卻繼續完整運作。
- 每個指標只用決策時已完成 K 線。`datetime` 是開盤標籤，`decision_time=datetime+1h`；最近已結算 funding 必須嚴格早於 `decision_time`，不使用同時點或未來結算值。所有事件保存數值、門檻、時點與資料截止棒。前綴截斷 11,000／16,000 棒需與完整回測已完成交易相等。
- 依下表順序跑，最多五輪。每輪主門檻及兩個鄰域在首次執行前鎖定；鄰域只能測穩健性，不能取代失敗中心。主門檻若不同時改善淨 PnL 與勝率，或 2／5bp 反轉，即停止該輪並進下一輪。需同時符合全期與較晚期、mark MDD／最差 30 日、30 個受影響獨立事件、15 個較晚期直接事件及鄰域才可能列入後續 shadow；歷史較晚期因多次舊研究已觸及，不是真正新的 OOS。實用目標為淨 PnL 至少 +5%、勝率至少 +1 百分點，並非統計顯著標準。

## 鎖定五輪

| 輪 | 指標與可得欄位 | 主門檻 | 鄰域 | 機制與主要風險 |
|---|---|---|---|---|
| 1 | RSI14：已完成 `close` 差分，Wilder 平滑；Short 取 `100-RSI` | 方向性 RSI ≥50 | 45／55 | 價格動能重新站回中線；可能只是短暫反彈。 |
| 2 | ADX14＋DI14：已完成 high／low／close，Wilder TR 與方向移動；`+DI` 或 `-DI` 須與原持倉方向一致 | ADX ≥20 | 15／25 | 趨勢強度與方向同時支持續抱；舊研究在進場 gate 測過 ADX，但未驗證到期續抱。 |
| 3 | 滾動 VWAP12：最近 12 根已完成棒的 `sum(typical_price*volume)/sum(volume)`；typical price=(H+L+C)/3 | Long close ≥VWAP、Short close ≤VWAP，方向性距離 ≥0 ATR | -0.15／+0.15 ATR | 價格仍在近期量加權重心的有利側；這是 K 線近似，非交易所逐筆 VWAP。舊持倉錨定 VWAP 提前退出研究屬不同決策。 |
| 4 | 最近**已結算且嚴格早於決策時間**的 ETHUSDT funding rate | Long funding ≤0；Short funding ≥0，即持倉側不支付 funding | 有利側需至少 0.5bp／容許不利側至 0.5bp | 檢驗成本與擁擠方向是否支持有限續抱；歷史 funding 不是實際帳戶結算，也不保證提供有效訊號。 |
| 5 | MFI14：typical price 與 volume 計算正／負 money flow；Short 取 `100-MFI` | 方向性 MFI ≥50 | 45／55 | 量價合成壓力支持續抱；可能高度重疊 RSI 與已拒絕的主動成交方向。 |

結果報告必須分開描述直接延長交易與因 occupancy／風控消失或新增的後續交易。五輪完成後即停止本批搜索，不因結果追加閾值、時數或第六個指標；任何正值也只能考慮未來 shadow research，不能上線。
